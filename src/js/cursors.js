import drag from '../img/move_32x32.png';
import noDrag from '../img/move-not_32x32.png';

const cursors = {
  auto: 'auto',
  drag: `url(${drag}) 15 15, pointer`,
  noDrag: `url(${noDrag}) 15 15, pointer`,
};

export default cursors;
